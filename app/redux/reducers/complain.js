import {GET_CONTACT_INFO} from '../constants/config';

const initialState = {
  complain: [],
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
